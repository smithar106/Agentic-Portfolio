# Optional: enables the "Ask Portfolio" conversational layer to use a real LLM.
# Without a key, the site runs in deterministic demo mode and answers only from
# structured portfolio data.
# LLM_PROVIDER=deepseek
# LLM_API_KEY=
# LLM_MODEL=deepseek-chat
